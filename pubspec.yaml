name: filman_flutter
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.3 <4.0.0'

dependencies:
  dio: ^5.4.2+1
  dynamic_color: ^1.7.0
  flutter:
    sdk: flutter
  html: ^0.15.4
  pod_player: ^0.2.2
  provider: ^6.1.2
  share_plus: ^8.0.3
  shared_preferences: ^2.2.2
  url_launcher: ^6.2.5
  video_player: ^2.1.15

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
